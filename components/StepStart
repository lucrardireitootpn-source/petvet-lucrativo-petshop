import React from "react";

interface StepStartProps {
  onStart: () => void;
}

export default function StepStart({ onStart }: StepStartProps) {
  return (
    <div className="p-8 text-center">
      <h1 className="text-xl font-bold mb-4">
        Diagnóstico Financeiro — PetShop
      </h1>
      <button
        onClick={onStart}
        className="w-full py-4 bg-blue-700 text-white rounded-lg font-bold"
      >
        Começar diagnóstico
      </button>
    </div>
  );
}
